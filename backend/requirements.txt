# Pinned loosely — scikit-learn is the one that matters. The .pkl files were
# written by joblib from whatever version trained them; if you get an
# InconsistentVersionWarning or a hard unpickling error, install the scikit-learn
# version your notebook used rather than the newest one.
flask>=3.0
flask-cors>=4.0
joblib>=1.3
numpy>=1.26
scikit-learn>=1.3
pandas>=2.0

# Network feature extraction. All three are optional: features.py degrades each
# lookup to -1 when its library is missing, which the model has seen in training.
requests>=2.31
dnspython>=2.4
python-whois>=0.9
